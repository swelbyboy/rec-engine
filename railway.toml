[build]
builder = "NIXPACKS"

[deploy]
startCommand = "/opt/venv/bin/uvicorn src.api:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
